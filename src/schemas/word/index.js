const wordSchema = require('./word-schema');
const wordResolvers = require('./word-resolvers');
const wordMutations = require('./word-mutations');

module.exports = {
	wordSchema,
	wordResolvers,
	wordMutations
}; 
